'use strict';

/**
 *  ethereum-minting-suggestion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ethereum-minting-suggestion.ethereum-minting-suggestion');

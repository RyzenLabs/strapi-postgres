'use strict';

/**
 * ethereum-minting-suggestion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ethereum-minting-suggestion.ethereum-minting-suggestion');

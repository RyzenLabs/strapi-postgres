'use strict';

/**
 * upcoming-collection service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::upcoming-collection.upcoming-collection');
